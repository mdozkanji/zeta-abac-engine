# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Required
RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
ATTRIBUTE_REGISTRY_ADDRESS=0x4004A83f8963B5943D08FE7FC4C3E550973C6362

# Optional (defaults shown)
PORT=4000
REDIS_URL=redis://localhost:6379
# Block number to start backfilling attribute events from. 0 scans the entire chain history,
# which is wasteful once you know the actual deployment block — set this to the block number
# GovernanceVoting/AttributeRegistry were deployed at (visible on the Etherscan links in the
# root README) to skip scanning blocks before the contract even existed.
SYNC_FROM_BLOCK=0
